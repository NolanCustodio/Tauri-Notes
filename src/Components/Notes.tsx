
import './Notes.css'

export default function Notes() {

    return (
        <input
            class='main'
            // onChange={(e) => setName(e.currentTarget.value)}s
            placeholder="Enter a name..."
        />

    )
}